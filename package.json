{
  "name": "car-a-day",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n web,api -c green,blue \"npm --workspace apps/web run dev -- --port 4200\" \"npm --workspace apps/api run dev\"",
    "dev:web": "npm --workspace apps/web run dev -- --port 4200",
    "dev:api": "npm --workspace apps/api run dev",
    "build": "npm --workspace apps/web run build && npm --workspace apps/api run build"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.6"
  }
}
